//buildscript {
  //  ext.kotlin_version = '1.7.10'
   // repositories {
     //   google()
       // mavenCentral()
   // }
    //dependencies {
      //  classpath 'com.android.tools.build:gradle:7.3.1' // Update to the latest version if necessary
       // classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.7.21" // Specify the Kotlin plugin version
        //classpath 'com.google.gms:google-services:4.4.2' // Ensure this version is compatible

    //}
//}
allprojects {
    repositories {
        google()
        mavenCentral()
    }
}


rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
